<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="veinhorn.sputnikpogrom" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name="com.sputnikpogrom.SPApplication">
        <activity
            android:name="com.sputnikpogrom.ui.NavigationDrawerActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:label="@string/app_name"
            android:theme="@style/NavigationDrawerTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/action_settings"
            android:name="com.sputnikpogrom.ui.settings.SettingsActivity"
            android:parentActivityName="com.sputnikpogrom.ui.NavigationDrawerActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.sputnikpogrom.ui.NavigationDrawerActivity" />
        </activity>

        <receiver android:name="com.sputnikpogrom.receivers.AlarmReceiver"/>
        <receiver android:name="com.sputnikpogrom.receivers.BootReceiver" android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <service android:name="com.sputnikpogrom.services.NotificationService" />
    </application>
</manifest>
